-- CREATE TABLE orders (
-- 	order_id SERIAL PRIMARY KEY,
--   person_id INTEGER,
--   product_name VARCHAR(30),
--   product_price FLOAT,
--   quantity FLOAT
-- )

-- INSERT INTO orders (person_id, product_name, product_price, quantity)
-- 	VALUES (2, 'Chicken Fingers', 10.99, 1),
--   (3, 'Cheesy Taco', 6.50, 2),
--   (4, 'Hot Wings', 5.00, 3),
--   (1, 'Sandwich', 6.75, 1),
--   (2, 'Mac n Cheese', 4.00, 1);

-- SELECT * FROM orders

-- SELECT COUNT(*) FROM orders

-- SELECT SUM(product_price) FROM orders

-- SELECT SUM(product_price) FROM orders
-- WHERE person_id = 2;
